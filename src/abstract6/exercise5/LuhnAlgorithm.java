package abstract6.exercise5;/*package week6.exercise5;

import java.util.Collections;
import java.util.List;

public class LuhnAlgorithm {
    public Integer calculate(Integer cardNumber){
        List<Integer> digits=getDigits(cardNumber);
        Collections.reverse(digits);
        return accumulate(digits);
    }

    private Integer accumulate(List<Integer> digits) {
    }

    private List<Integer> getDigits(Integer cardNumber) {
        String[] split=cardNumber.split("");
    }
}*/
